

database_schemes.py

role type: change schema from list(valid_roles) → str (validator checks types, not allowed‑values lists). 
borrower_id type: make int (aligns with user IDs used elsewhere). 
Cross‑file contract: services must use user_id (not id) and respect user_profile.user_name nesting defined here. 


json_files_major_services.py

Sorting bug: don’t assign .sort() result; iterate the list after records_to_remove.sort(reverse=True). 
Update loop: replace for record in enumerate(file_content): with for record in file_content:; the current code treats (index, value) tuple as a dict. 
Validation placement: remove_from_file calls validate_file_data() before persisting; it reads from disk, so it validates stale data—move validation after write_json_data() or validate the in‑memory list per record.
Return types in docs: create_backup_file() returns a Path (docstring says str)—align docstring. 


book_json_file_service.py

File path wiring: in __init__, set self.json_service.file_path = self.file_path so the underlying service reads the correct file.
Validator return misuse: don’t append validated_book_data (a success string); call validator for side‑effect then current_data.append(book_data).
get_all_books_list: don’t raise inside the loop; collect books and raise only if the final list is empty.
update_book_data:

Update only the book with matching book_id.
Don’t validate the list against a dict schema; after updating, call the utility’s validate_file_data() (no args) and then write_json_data().


user_json_file_service.py

File path wiring: in __init__, set self.json_service.file_path = self.file_path.
Field names: use user["user_id"] everywhere (get/add/update/delete); current code mixes id vs user_id.
Nested field: avoid gating with user["user_name"]; schema nests name under user["user_profile"]["user_name"]. Prefer returning all users (or check user.get("user_id")).
Validator return misuse: don’t append the validator’s return; call validator, then append user_data.
Wrong validator call: use self.json_service.validate_file_data() (no parameters) after edits, not validate_file_data(current_data, schema).
Update loop: filter by user_id and don’t raise just because the first record lacks the field—only raise if no matching user was updated.


loan_json_file_service.py

File path wiring: in __init__, set self.json_service.file_path = self.file_path.
Copy‑paste messages: replace “Book data …” → “Loan data …” in add_loan_data and related errors.
get_all_loans_list: remove the unused loan_id parameter and don’t raise per record; collect all and raise only if the final list is empty.
update_file_data: update only the record with matching loan_id; after modifying, call validate_file_data() (no args) then write_json_data(). Also avoid validating the entire list against a dict schema.
Delete by key: use loan["loan_id"] (not loan["id"]) when removing a record.

